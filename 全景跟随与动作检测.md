# 全景跟随与动作检测（`pano/`）

把两件事接起来：

* **`南客松-v2/x5-link`**：X5 的 Webcam 模式吐 **2880×1440 等距柱状（经纬图）全景**，
  机内已拼接 + 防抖
* **`GestureMate`**：手势 / 面部 / 身体的动作检测

需求是两句：**① 跟随我；② 做我的动作检测**，前提是**我可能出现在全景的任意位置**。

```bash
# 把一个文件夹里的全景帧跑成「跟着人 + 对该人动作检测」的结果
.venv/bin/python -m pano --frames /Users/imac/南客松-v2/3dgs-lab/proj_erp/frames \
    --out /tmp/pano_out --limit 12
```

输出：`pano_00000.png`（抽出的透视视角 + 骨架标注）与 `tracks.json`（每帧的
yaw/pitch/fov/score + 动作特征）。

---

## 一、为什么不能"直接把全景丢给检测器"

人形检测器是在**透视照片**上训练的。等距柱状图左右两端被水平拉伸得最厉害
（越靠边越夸张），人在那儿会被拉成畸形，检不出来；只有全景正中那一条近似透视。

所以本方案**永远只把检测器喂给"已经矫正过的透视视角"**，全景本身只干一件事：
**用来抽视角**。这也正是 x5-link README 里那句「从这张全景图里按朝向抽透视视角即可」
的落地方式 —— 原话还补了一句「体感识别出的人体朝向可以直接驱动这个视角」，
本模块做的就是这件事，而且是闭环的。

## 二、两步怎么落地

### 第 1 步：跟随（找 + 跟住）

    if 未锁定:  在**球面**上粗扫（yaw x pitch）-> 合并同一个人的重复候选 -> 附近细化
                -> 拿"真正要用的那个视角"验证 -> 验过的才锁
    else:       只在当前方位附近抽 1 个视角 -> 跟住；连续跟丢 K 帧才回到上面重新找

* **找人**用 `pose.Pose(complexity=0, static_image_mode=True)` 跑**小图**
  （默认 224×224），快；全扫是"偶尔"发生的，不是每帧。
  **必须是无状态检出器**（理由见第六节踩坑 5）。
* **扫描范围是整球，不是地平线**：默认 pitch 取 `-45/-10/25/60`，配 fov 90°
  覆盖约 -90°..+105°。X5 平放在桌上时人是**俯身从上方入镜**的，只扫地平线
  一个人都找不到（实测两个人分别在 pitch +52° / +78°）。
* **验证过才锁**：粗扫出的最高分不一定是检测器能用的视角。所以候选要用
  **最终的 view_size + fov** 复核一遍，并且用**下游真正要跑的引擎**（holistic）
  再确认一次；拿不到脸就退而求其次接受"只有身体"，全验不过就老实说没找到。
* **跟住**用圆周平滑（在 sin/cos 上做 EMA —— 直接对角度做 EMA 会在 ±180° 交界翻车：
  359° 和 1° 只差 2°，平均却给 180°）。pitch 是 [-90,90] 上的连续量，用线性平滑。
* **fov 自适应**：按"人在画面里占多高"调视场角，让他**远近都保持差不多大小**。
  ⚠️ 只在**人整个在画面里**时才调 —— 被画面裁到时量到的跨度已经不代表他的真实
  尺寸，用它去调会两头翻车（见第六节踩坑 6）。
* **得分**不只看可见度：还乘上"人在画面里的占比"，否则一个只露半个身子、
  贴在画面边缘的人也会得高分。另外对**近天顶方位降权**，因为那里的透视视角
  是病态的（见第六节踩坑 4）。

### 第 2 步：动作检测

锁定后把该方位的那一块抽成正常透视画面（`512×512`），
**喂给 GestureMate 原有的管线**：

    LandmarkEngine（holistic：脸 + 身 + 手） -> face_body 语义（眨眼/嘴巴/头朝向/举手）

所以手势、眨眼、举手、身体姿态这些**一行都不用重写** —— 它们本来就是
在"一张正常的透视画面"上工作的，跟随只是负责把这张画面准备好。

## 三、实测

`tests/test_pano_follow.py`（6 项，全绿）：

| 测试 | 结果 |
|---|---|
| 视角↔全景 投影往返一致 | 5 个点误差 **< 1e-6** |
| 全景像素→角度约定 | 中心 0°、左边缘 −180°、上边缘 +90° |
| 角度圆周平滑 | 359°/1° → 0°（不是 180°），0°/90° → 45° |
| **任意方位都能找到** | 贴 +70/−110/0/**155**°，**最大误差 1.4°** |
| 空场景不误锁 | 没有人就老实说没有 |
| 抽出的视角上跑通动作检测 | 脸 468 点 + 身体 + 手 + 语义特征 |

> 关于"任意方位"这条怎么测的：跟随准不准，拿真实全景肉眼看是**没有 ground truth** 的
> （你不知道人在第几度）。所以反过来造数据 —— 把一张**确实能被检出人**的普通透视照片，
> 用 `paint_perspective_into_equirect()` 贴进全景的**指定方位**，再断言能不能找回来。
> 这就是 `pano/projection.py` 里那个"绘制"函数存在的唯一理由。

视觉对照（2048×1024 全景，人贴在 **−95°**）：

| 做法 | 结果 |
|---|---|
| 不跟随（固定看 0°） | **全空**，只有地板/天空渐变 —— 人完全不在视野里 |
| **跟随** | 找到 **−93.9°**（误差 **1.1°**），居中抽出，检出脸 468 点 + 骨架 + **举手 left=True** |

`pano/__main__.py` 跑那批真实的 `proj_erp/frames`（Minecraft 合成图，画面里没有人）
时逐帧报 `丢失 score=0.00` —— **不误报**，这点比"总能找到一个"更重要。

### 真机（X5 实拍）—— 合成数据测不出来的问题

上面全是**合成**数据（把一张透视图贴进素色全景）。它证明不了真机可用。
拿 X5 实拍的 2880×1440 帧补验之后，原来的实现对**画面里明明存在的两个人
一帧都锁不住**（`locked=False`，score 0.26 < 阈值 0.35）。修完之后的对比：

| | 修之前 | 修之后 |
|---|---|---|
| 锁定 | ❌ False | ✅ True |
| yaw / pitch | — | **+101.8° / +48.4°** |
| score | 0.26 | **0.85** |
| 脸 / 身体关键点 | 0 / 0 | **468 / 33** |
| 语义特征 | `available: False` | 眨眼、嘴巴、头朝向、举手、身体倾斜 |
| 定位 / 检测 | 309ms / 0ms | 2263ms（首次找人）/ 32ms |
| 跟住一帧 | — | **74ms 定位 + 29ms 检测** |

修复点全部落在 `pano/follow.py`，并且用
`tests/test_pano_real_frame.py`（4 项）把每一条钉住：

| 测试 | 钉住的是什么 |
|---|---|
| 探针无状态 | 同一视角两次探针分数必须**一模一样**（曾经 0.26 vs 0.77） |
| 真机帧锁得住 | 必须锁定 + 出脸 + 出身体 + 出语义 |
| 人不在水平线上 | 找到的人在 pitch +48°，|pitch|>35° |
| 跟住不掉锁 | 第二帧不许重新找人，且明显更快 |

> 真机帧存在 `pano/demo/real_x5/`，可以直接看：
> `0_X5实机原始帧_2880x1440.jpg` 是原图；`2_锁到yaw+131_pitch+77抽出的视角.png`
> 是修之前"分数最高但下游检不出"的那个坏视角的对照。


## 四、开销

真机（2880×1440 帧，M2 Max）实测：

| 阶段 | 耗时 |
|---|---|
| 冷启动找人（球面粗扫 8×3 = **24 个视角** + 细化 + 验证） | **~1.6–2.2 s**，只在从没锁过时发生 |
| 跟丢后再找人（**先试上次方位附近的 3×3**） | **~0.6 s**（局部成功时；失败才退回全球面） |
| 跟住一帧（抽视角 + 一次 holistic） | **~40–60 ms** |
| 动作检测（holistic，384×384） | **~30–45 ms** |
| 整条实时循环（常驻取流，检测每帧） | **~90 ms ≈ 11 FPS** |
| 同上，但 `--detect-every 2` | **~65 ms ≈ 13–15 FPS** |

> 上面的绝对数字会随机器负载明显变化（同一份代码实测在 3.4–11 FPS 之间浮动过，
> 因为同时跑着 Steam 之类的重进程）。**看相对变化，别看绝对值。**

### 延迟是怎么从 ~3.4 FPS 优化到 ~11–15 FPS 的

一开始实时循环只有 **3.4 FPS**（约 325ms/帧）。先量了预算，再动手 ——
**量出来的瓶颈和一开始猜的**两次**都不一样**：

| 环节 | 实测 | 结论 |
|---|---|---|
| 抓一帧 snapshot（324KB） | 18–28 ms | 次要 |
| 解码 2880×1440 | 8–15 ms | **不是瓶颈**（原来猜 60–80ms，错了） |
| 解码 1/2（1440×720） | 7–9 ms | 只省几毫秒，不值得 |
| **pose 探针 512px（成功检出时）** | **116 ms** | ← 第一真凶 |
| holistic | 30–45 ms | 后来的地板 |
| **`equirect_to_perspective` 抽一个 512 视角** | **76 ms** | ← 第二真凶（原来完全没怀疑它） |

两条关键认识：

1. **代价由"跑了几次 MediaPipe"决定，不由像素多少决定** —— 128px 和 256px 的粗扫
   耗时几乎一样；把输入整体降到 1/2 反而更慢。
2. **抽视角本身和"跑模型"一样贵**。`_bilinear` 原来每次都把整张 2880×1440×3 的源图
   `astype(np.float32)`（约 149MB 临时内存），光这一步就 76ms。

对应的六条改动：

1. **跟住的那一帧不再单独跑 pose 探针**。下游 holistic 这一帧本来就检出了身体，
   直接拿它的关键点反推"人在哪、多大"来闭环（`_absorb_detect`）。
   每帧从"2 次 pose + 1 次 holistic"变成**只有 1 次 holistic**：
   稳态 **292ms → 100ms**。检不到身体时才退回探针兜底。
2. **抽视角改用 `cv2.remap`**（`projection.py::_bilinear`）。在 C++ 里直接吃 uint8、
   定点插值，不再把整张源图转 float。512 视角 **75.9ms → 41.7ms**。
3. **跟随视角默认 512 → 384**。抽视角的开销随输出尺寸陡涨，而**检测质量几乎不变**
   （192/256/320/384/512 全都稳定检出 face 468 + pose 33）：
   384 的抽取只要 **10.4ms**（512 是 41.7ms）。
4. **取帧 + 解码挪到常驻后台线程**（`live.py::StreamReader`），一直读 x5-link 的
   MJPEG 并只保留最新一帧。主循环因此拿到的是现成 ndarray，
   **"取到一张图"的 30–45ms 从关键路径上消失了**（改为并行发生，并按帧率限速解码）。
5. **跟丢先试局部，不行才全球面**。跟丢绝大多数只是抖动/短暂遮挡，人还在原方位附近。
   局部 3×3 约 0.6s 就能拿回 score 0.93，而全球面要 ~3.0s。
6. **粗扫采样 48 → 24 次**（12×4 改成 8×3，俯仰 `-40/5/50` 配 fov 90° 覆盖约
   -85°..+95°）。冷启动 **2.1–2.7s → 1.4–1.9s**，而且锁到的是**同一个人、分数更高**
   （0.96 vs 0.85）。

还有一个**可选的**提速旋钮 `--detect-every N`：抽视角和画图每帧都做，但动作检测
每 N 帧才跑一次（跳过的帧复用上一帧的 landmarks，并在 `PanoResult.detected` 里
如实标 False）。N=2 实测 **11 FPS → 13–15 FPS**。默认保持 1（语义每帧刷新），
因为"眨眼/举手以一半频率刷新"是个功能取舍，不该由代码偷偷替用户决定。

> ⚠️ 代价与残留：
> * 每帧只剩一次检测，意味着**跟随用的就是那份检测结果** —— 检不出身体的那一帧
>   跟不动（会退回探针兜底）。`--detect-every 2` 时跳过的帧也不再闭环校正方位。
> * "人根本不在画面里"时，跟随**不会立刻承认**：`max_misses` 从 8 降到 4，
>   但在 ~90ms/帧下仍有约 0.4s 的"锁着但 score=0"。所以状态栏会把
>   **跟丢计数**显式打出来（"跟丢中（连续 N 帧没检出人）"），不装作在跟。
> * 空旷**但杂乱**的全景上 pose 会幻觉出人（实测在一个**铺好的床**上，holistic 把
>   被子褶子检成一具 33 点的"身体"，而且 7/7 锚点全在画面内）。所以快路径的分数
>   必须用**躯干真实可见度**来算，不能只用"关节有几个在画面内"—— 后者会给这种
>   幻觉打满分 1.00。已加 0.3 可见度门槛，把反推方位所需最少锚点从 3 提到 4，
>   但没有根治。`test_no_false_positive` 只挡住纯色图，挡不住杂乱图。

### 顺带修掉的一个测试脆弱点

`tests/test_pano_follow.py` 的合成全景原来是 1024×512，fov 70° 的一屏只覆盖
`1024*70/360 ≈ 199px`，而跟随要抽出 **512px** 视角 —— 等于把脸放大 2.6 倍，
人脸网格检测卡在临界点上：插值差 1 个灰度值就能让"检不检出脸"翻转
（换 `cv2.remap` 后就真的翻了）。已把合成全景提到 **2048×1024**（同样一屏覆盖
≈398px，放大 1.3 倍），测的才是"抽出的视角能不能跑动作检测"，
而不是"mediapipe 能不能认出一张 2.6 倍糊掉的脸"。




## 五、文件

| 文件 | 作用 |
|---|---|
| `pano/projection.py` | 等距柱状 ↔ 透视 的投影数学。核心取自 `3dgs-lab/x360/projection.py`（已数值验证），**按原样复制**而非跨项目 import（跨项目依赖太脆），并补了两个反向映射 |
| `pano/follow.py` | `PanoFollower`：找人 / 跟住 / 动作检测；`annotate()` 画结果 |
| `pano/__main__.py` | 命令行：文件夹或视频 -> 带标注的透视视角 + `tracks.json` |
| `tests/test_pano_follow.py` | 6 项**合成**数据的测试（带 ground truth 的方位精度） |
| `tests/test_pano_real_frame.py` | 4 项**真机帧**的测试（锁得住 / 探针无状态 / 人不在水平线 / 跟住不掉锁） |
| `pano/demo/real_x5/` | X5 实拍原始帧 + 诊断对照图（修之前那些坏视角长什么样） |

## 六、踩坑

1. **不要对角度直接做 EMA**：359° 和 1° 平均成 180°，跟随会突然甩到对面。
   要在 (sin, cos) 上平滑再 `atan2` 回来。
2. **等距柱状的中心在半个像素上**：`x = (lon/2π+0.5)·(w−1)`，`lon=0` 对应
   `(w−1)/2 = 511.5`，整数 512 其实偏了 0.176°。第一版测试就是在这里假失败的。
3. **`pose` 与 `holistic` 的坐标系约定不一样**：前者输出的是"普通透视图像坐标"，
   后者在 GestureMate 里被 `extractLandmarks` 做过镜像补偿。本模块**只对抽出的
   透视视角做检测**，所以走 `LandmarkEngine`/`extractLandmarks` 原有那条路，
   没有引入新的坐标转换 —— 少一层转换就少一类 bug。
4. **找人的得分要乘"占比"**：只看关节可见度的话，画面边缘露半个身子的人也会高分，
   跟随会往边缘飘。
5. **探针检出器必须无状态**（真机才暴露）。原来 `_probe` 复用一个
   `pose.Pose(static_image_mode=False)`（视频**跟踪**模式）去扫一堆互不相关的
   视角，于是上一次的跟踪状态会漏进来。实测**同一张输入**，作为第 1 次调用
   score=0.26、作为第 13 次调用 score=0.77 —— 找人依赖"扫的顺序"，不可靠。
   现在找人/跟随一律用 `static_image_mode=True`。
6. **fov 拟合只在人整个在画面里时才做**（真机才暴露）。用被画面裁到的跨度去调
   fov 会两头翻车，两个方向都实测到了：
   只用画面内的关节 → 收紧 → 裁更多 → 跨度更小 → 一路崩到 `fov_min`（实测 51.8）；
   连画面外的关节一起用 → 越界点把跨度撑爆 → 一路顶到 `fov_max`（实测 95.0）。
   现在"裁到就不调"，fov 保持不变。
7. **锁定要用下游真正要跑的那个引擎验一遍**（真机才暴露）。分数最高的方位不一定是
   检测器能用的方位：实测最高分 0.996 落在一个**被大手臂占满的近天顶视角**上，
   而那个视角下游一个点都检不出来。更隐蔽的是**验证的视角和锁定的视角差了几度**
   （实测 2.4°），acquire 说"验过了有脸"、紧接着 `step()` 却 `face: 0`。
   现在验证就在**最终会锁的那个视角**上做。
8. **近天顶视角是病态的**：`view_basis()` 里 `right = forward x world_up`，
   pitch→±90° 时 `right` 长度→0，抽出来的画面会乱滚、检测器检不出
   （实测同一方位 fov=90 能检出脸 468，fov=70 冷启动检出 0）。
   所以对近天顶方位降权，并把 pitch 夹在 `pitch_limit`（默认 75°）内。
9. **别把 pitch 硬夹到 ±35°**：X5 平放在桌上时人本来就在 +50°..+80°，
   夹回地平线等于把刚找到的人丢掉。
10. **MediaPipe 会把关节外推到画面外**（实测髋部 y=1.66）。画面外的点有两处坏处：
    会撑大"占比"项（分数全挤在 1.0 附近），反投影回全景的方向也已经不成立。
    所以打分和反投影都只用画面内的关节。
11. **`Fix.acquired` 要清掉**：`track()` 是原地改 `self.fix` 的，原来没把
    `acquired` 置回 False，于是它永远停在第一次锁定时的 True，调用方分不清
    "刚重新找到"和"一直跟住"。
12. **下游 `LandmarkEngine`（holistic）也是有状态的跟踪器**：同一张图，
    冷引擎检 0 个点、被前一帧"预热"过的引擎能检出 468 个。所以
    「这个视角能不能检测」这件事必须按序列来判，不能指望单帧冷启动。
13. **性能要先量再改，别猜**。这次的瓶颈和直觉相反：不是解码（8–15ms），
    而是"跑了几次 MediaPipe"（一次成功的 512px pose 要 116ms）。
    同样的原因，**把输入降采样到 1/2 并不会变快**（实测反而 100ms → 192ms），
    因为代价在模型调用上，不在像素上。
14. **每帧只跑一次检测，别重复问同一个问题**。原来稳态每帧跑
    "512px pose 探针 + 224px fov 拟合 + 512px holistic"，其中 pose 探针问的
    "人在不在" 和 holistic 的答案重复。改成用 holistic 的身体关键点反推方位与尺寸后，
    稳态 292ms → 100ms。**同一个问题问两遍是这个项目里最大的一笔浪费。**
15. **"锁着"不等于"跟到了"**。当人不在画面里时，跟随会保持 locked 直到
    `max_misses` 帧。这个窗口要么缩短（8→4），要么**在界面上显式说出跟丢计数** ——
    否则就会出现"状态显示已锁定、实际 score=0"这种骗人的画面。
16. **别让 `astype(np.float32)` 出现在每帧路径上**。`_bilinear` 原来每次都把整张
    2880×1440×3 的源图转成 float（约 149MB 临时内存），光这一步就占 76ms ——
    比跑一次神经网络还贵。**重采样交给 `cv2.remap`**（C++、uint8、定点插值），
    同样的活只要几毫秒。教训：**"取个色"也可能比"跑模型"贵**，别只盯着模型。
17. **跟随视角的尺寸是性能旋钮，不是质量旋钮**。输出从 512 降到 384，抽取耗时
    41.7ms→10.4ms，而 face 468 / pose 33 在 192–512 全都稳定检出。花点时间扫一遍
    "尺寸 vs 耗时 vs 检出"，往往能白捡一大截帧率。
18. **取帧别每帧重新发一次 HTTP**。每帧抓一次 `snapshot.jpg` 要重建连接、重下 324KB，
    20–30ms 干等。改成**常驻线程读 MJPEG、只留最新一帧**，这段开销就从关键路径上
    彻底消失了（并行发生）。这是"流水线化"最便宜的一刀。




## 七、接到实时 X5 上

本模块对输入只要求"一帧等距柱状图"，所以换成实时流不用改代码：

* x5-link 的 `web --port 8765` 已经把实时画面以 MJPEG 推到 HTTP 上。
  **已经实测过这条路**：`curl -s http://127.0.0.1:8765/snapshot.jpg -o f.jpg`
  拿到的就是一张真正的 2880×1440 等距柱状帧，直接喂给 `PanoFollower.step()` 即可
  （本文档里所有"真机"数据都是这么取的）；
* 或者 `preview --record` 存下 mp4，用 `--video` 直接跑。

所以"接实时流"这件事**只差一个循环**：

```bash
# 每帧抓一张 snapshot 喂进去（最省事的实时接法）
while true; do
  curl -s http://127.0.0.1:8765/snapshot.jpg -o /tmp/f.jpg && \
  .venv/bin/python -c "
import cv2, sys; sys.path[:0]=['.','webui']
from pano.follow import PanoFollower
f = PanoFollower(); r = f.step(cv2.imread('/tmp/f.jpg'))
print(r.fix.locked, r.fix.yaw, r.fix.pitch, (r.features or {}).get('landmarkCount'))"
done
```

> ⚠️ 上面是"每帧新建 follower"的示意，实际用要复用同一个 `PanoFollower`
> （否则每帧都在重新找人，2 秒一帧）。真正的实时循环应该是一个常驻进程。

**还没做的**：

* 常驻的实时取流循环（把 `snapshot.jpg` 或 MJPEG 流接成 `step()` 的帧源）；
* 把跟随的 yaw/pitch 反喂给网页端那个 360° 视口（`x5link/viewer.html`），
  做到"网页上自动跟着人转"。那需要改 x5-link 那边，本次没有动。
* **视角的"翻滚"还没有解**：X5 在桌上时它的"上"不等于重力的"上"，抽出来的画面
  是歪的（实测画面里的人体轴相对画面竖直线偏了约 20°，天地线也是斜的）。
  这会让 `face_body` 里"身体倾斜/头 roll"这类**方向性语义是相对歪画面的**，
  数值不可直接信。要修得先估出重力方向（候选做法：用人的肩→髋轴当重力参考、
  或者从地平线/消失点估），这件事**第 ② 步（在房间平面/墙面上标注）必须要做**，
  因为"贴到地面/墙面上"本身就要求一个重力对齐的坐标系。

